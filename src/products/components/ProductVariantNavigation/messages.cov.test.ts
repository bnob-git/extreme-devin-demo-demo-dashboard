import { messages } from "./messages";

describe("products/components/ProductVariantNavigation/messages.ts", () => {
  it("should export messages", () => {
    expect(messages).toBeDefined();
  });
});
