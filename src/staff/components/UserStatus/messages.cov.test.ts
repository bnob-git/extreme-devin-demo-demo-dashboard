import { userStatusMessages } from "./messages";

describe("staff/components/UserStatus/messages.ts", () => {
  it("should export userStatusMessages", () => {
    expect(userStatusMessages).toBeDefined();
  });
});
