import { useStyles } from "./styles";

describe("shipping/components/ShippingZoneCountriesAssignDialog/styles.ts", () => {
  it("should export useStyles", () => {
    expect(useStyles).toBeDefined();
  });
});
