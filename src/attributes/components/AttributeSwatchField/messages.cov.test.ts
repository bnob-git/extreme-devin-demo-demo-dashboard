import { swatchFieldMessages } from "./messages";

describe("attributes/components/AttributeSwatchField/messages.ts", () => {
  it("should export swatchFieldMessages", () => {
    expect(swatchFieldMessages).toBeDefined();
  });
});
