import { useChannelsSearch } from "./useChannelsSearch";

describe("hooks/useChannelsSearch.ts", () => {
  it("should export useChannelsSearch", () => {
    expect(useChannelsSearch).toBeDefined();
  });
});
