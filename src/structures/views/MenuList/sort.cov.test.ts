import { getSortQueryVariables } from "./sort";

describe("structures/views/MenuList/sort.ts", () => {
  it("should export getSortQueryVariables", () => {
    expect(getSortQueryVariables).toBeDefined();
  });
});
