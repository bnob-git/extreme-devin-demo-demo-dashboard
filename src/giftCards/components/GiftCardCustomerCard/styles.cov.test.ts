import { useListWrapperStyles } from "./styles";

describe("giftCards/components/GiftCardCustomerCard/styles.ts", () => {
  it("should export useListWrapperStyles", () => {
    expect(useListWrapperStyles).toBeDefined();
  });
});
