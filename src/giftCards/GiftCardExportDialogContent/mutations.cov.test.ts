import { exportGiftCards } from "./mutations";

describe("giftCards/GiftCardExportDialogContent/mutations.ts", () => {
  it("should export exportGiftCards", () => {
    expect(exportGiftCards).toBeDefined();
  });
});
