import { giftCardBulkCreateErrorKeys } from "./types";

describe("giftCards/GiftCardBulkCreateDialog/types.ts", () => {
  it("should export giftCardBulkCreateErrorKeys", () => {
    expect(giftCardBulkCreateErrorKeys).toBeDefined();
  });
});
