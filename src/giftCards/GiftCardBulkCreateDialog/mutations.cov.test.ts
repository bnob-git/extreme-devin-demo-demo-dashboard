import { giftCardBulkCreate } from "./mutations";

describe("giftCards/GiftCardBulkCreateDialog/mutations.ts", () => {
  it("should export giftCardBulkCreate", () => {
    expect(giftCardBulkCreate).toBeDefined();
  });
});
