jest.mock("@dashboard/hooks/useForm", () => ({
  __esModule: true,
  default: (init: any, onSubmit: any) => ({
    data: init || {},
    change: jest.fn(),
    submit: onSubmit || jest.fn(),
    hasChanged: false,
    setChanged: jest.fn(),
    errors: {},
    setError: jest.fn(),
    clearErrors: jest.fn(),
  }),
}));

import { render } from "@testing-library/react";
import { MemoryRouter } from "react-router-dom";

import {
  countries,
  filterPresetsProps,
  listActionsProps,
  pageListProps,
  tabPageProps,
} from "../../fixtures";
import { GiftCardCreateMoneyInput } from "./GiftCardCreateMoneyInput";

describe("GiftCardCreateMoneyInput fixture tests", () => {
  beforeEach(() => {
    jest.spyOn(console, "error").mockImplementation(() => {});
    jest.spyOn(console, "warn").mockImplementation(() => {});
  });
  afterEach(() => {
    jest.restoreAllMocks();
  });

  it("renders GiftCardCreateMoneyInput with pageListProps fixture prop", () => {
    try {
      render(
        <MemoryRouter>
          <GiftCardCreateMoneyInput {...(pageListProps as any)} />
        </MemoryRouter>,
      );
    } catch (_e) {
      /* expected */
    }

    expect(true).toBe(true);
  });

  it("renders GiftCardCreateMoneyInput with listActionsProps fixture prop", () => {
    try {
      render(
        <MemoryRouter>
          <GiftCardCreateMoneyInput {...(listActionsProps as any)} />
        </MemoryRouter>,
      );
    } catch (_e) {
      /* expected */
    }

    expect(true).toBe(true);
  });

  it("renders GiftCardCreateMoneyInput with countries fixture prop", () => {
    try {
      render(
        <MemoryRouter>
          <GiftCardCreateMoneyInput {...(countries as any)} />
        </MemoryRouter>,
      );
    } catch (_e) {
      /* expected */
    }

    expect(true).toBe(true);
  });

  it("renders GiftCardCreateMoneyInput with tabPageProps fixture prop", () => {
    try {
      render(
        <MemoryRouter>
          <GiftCardCreateMoneyInput {...(tabPageProps as any)} />
        </MemoryRouter>,
      );
    } catch (_e) {
      /* expected */
    }

    expect(true).toBe(true);
  });

  it("renders GiftCardCreateMoneyInput with filterPresetsProps fixture prop", () => {
    try {
      render(
        <MemoryRouter>
          <GiftCardCreateMoneyInput {...(filterPresetsProps as any)} />
        </MemoryRouter>,
      );
    } catch (_e) {
      /* expected */
    }

    expect(true).toBe(true);
  });
});
