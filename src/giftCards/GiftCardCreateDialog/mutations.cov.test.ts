import { giftCardCreate } from "./mutations";

describe("giftCards/GiftCardCreateDialog/mutations.ts", () => {
  it("should export giftCardCreate", () => {
    expect(giftCardCreate).toBeDefined();
  });
});
