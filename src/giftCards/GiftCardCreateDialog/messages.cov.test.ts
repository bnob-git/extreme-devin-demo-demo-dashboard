import { giftCardCreateMessages } from "./messages";

describe("giftCards/GiftCardCreateDialog/messages.ts", () => {
  it("should export giftCardCreateMessages", () => {
    expect(giftCardCreateMessages).toBeDefined();
  });
});
