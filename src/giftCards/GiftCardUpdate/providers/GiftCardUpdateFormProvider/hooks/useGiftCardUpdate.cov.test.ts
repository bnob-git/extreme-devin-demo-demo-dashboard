import defaultExport from "./useGiftCardUpdate";

describe("giftCards/GiftCardUpdate/providers/GiftCardUpdateFormProvider/hooks/useGiftCardUpdate.ts", () => {
  it("should have default export", () => {
    expect(defaultExport).toBeDefined();
  });
});
