import defaultExport from "./useGiftCardUpdateDialogs";

describe("giftCards/GiftCardUpdate/providers/GiftCardUpdateDialogsProvider/hooks/useGiftCardUpdateDialogs.ts", () => {
  it("should have default export", () => {
    expect(defaultExport).toBeDefined();
  });
});
