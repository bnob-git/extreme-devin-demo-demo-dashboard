import defaultExport from "./styles";

describe("giftCards/GiftCardUpdate/GiftCardUpdatePageHeader/styles.ts", () => {
  it("should have default export", () => {
    expect(defaultExport).toBeDefined();
  });
});
