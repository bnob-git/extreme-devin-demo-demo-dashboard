import { GiftCardUpdatePageActionParamsEnum, PLACEHOLDER } from "./types";

describe("giftCards/GiftCardUpdate/types.ts", () => {
  it("should export GiftCardUpdatePageActionParamsEnum", () => {
    expect(GiftCardUpdatePageActionParamsEnum).toBeDefined();
  });

  it("should export PLACEHOLDER", () => {
    expect(PLACEHOLDER).toBeDefined();
  });
});
