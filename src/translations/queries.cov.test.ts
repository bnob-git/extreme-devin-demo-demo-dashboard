import {
  attributeTranslationDetails,
  attributeTranslations,
  categoryTranslationDetails,
  categoryTranslations,
  collectionTranslationDetails,
  collectionTranslations,
  menuItemTranslationDetails,
  menuItemTranslations,
  pageTranslationDetails,
  pageTranslations,
  productTranslationDetails,
  productTranslations,
  productVariantList,
  productVariantTranslationDetails,
  saleTranslationDetails,
  saleTranslations,
  shippingMethodTranslationDetails,
  shippingMethodTranslations,
  voucherTranslationDetails,
  voucherTranslations,
} from "./queries";

describe("translations/queries.ts", () => {
  it("should export attributeTranslationDetails", () => {
    expect(attributeTranslationDetails).toBeDefined();
  });

  it("should export attributeTranslations", () => {
    expect(attributeTranslations).toBeDefined();
  });

  it("should export categoryTranslationDetails", () => {
    expect(categoryTranslationDetails).toBeDefined();
  });

  it("should export categoryTranslations", () => {
    expect(categoryTranslations).toBeDefined();
  });

  it("should export collectionTranslationDetails", () => {
    expect(collectionTranslationDetails).toBeDefined();
  });

  it("should export collectionTranslations", () => {
    expect(collectionTranslations).toBeDefined();
  });

  it("should export menuItemTranslationDetails", () => {
    expect(menuItemTranslationDetails).toBeDefined();
  });

  it("should export menuItemTranslations", () => {
    expect(menuItemTranslations).toBeDefined();
  });

  it("should export pageTranslationDetails", () => {
    expect(pageTranslationDetails).toBeDefined();
  });

  it("should export pageTranslations", () => {
    expect(pageTranslations).toBeDefined();
  });

  it("should export productTranslationDetails", () => {
    expect(productTranslationDetails).toBeDefined();
  });

  it("should export productTranslations", () => {
    expect(productTranslations).toBeDefined();
  });

  it("should export productVariantList", () => {
    expect(productVariantList).toBeDefined();
  });

  it("should export productVariantTranslationDetails", () => {
    expect(productVariantTranslationDetails).toBeDefined();
  });

  it("should export saleTranslationDetails", () => {
    expect(saleTranslationDetails).toBeDefined();
  });

  it("should export saleTranslations", () => {
    expect(saleTranslations).toBeDefined();
  });

  it("should export shippingMethodTranslationDetails", () => {
    expect(shippingMethodTranslationDetails).toBeDefined();
  });

  it("should export shippingMethodTranslations", () => {
    expect(shippingMethodTranslations).toBeDefined();
  });

  it("should export voucherTranslationDetails", () => {
    expect(voucherTranslationDetails).toBeDefined();
  });

  it("should export voucherTranslations", () => {
    expect(voucherTranslations).toBeDefined();
  });
});
