import defaultExport from "./attributeValueSearchHandler";

describe("utils/handlers/attributeValueSearchHandler.ts", () => {
  it("should have default export", () => {
    expect(defaultExport).toBeDefined();
  });
});
