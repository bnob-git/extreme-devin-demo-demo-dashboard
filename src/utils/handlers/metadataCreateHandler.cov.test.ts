import defaultExport from "./metadataCreateHandler";

describe("utils/handlers/metadataCreateHandler.ts", () => {
  it("should have default export", () => {
    expect(defaultExport).toBeDefined();
  });
});
