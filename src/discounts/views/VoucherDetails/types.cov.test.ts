import { VOUCHER_UPDATE_FORM_ID } from "./types";

describe("discounts/views/VoucherDetails/types.ts", () => {
  it("should export VOUCHER_UPDATE_FORM_ID", () => {
    expect(VOUCHER_UPDATE_FORM_ID).toBeDefined();
  });
});
