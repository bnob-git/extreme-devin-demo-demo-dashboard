import { VOUCHER_CREATE_FORM_ID } from "./types";

describe("discounts/views/VoucherCreate/types.ts", () => {
  it("should export VOUCHER_CREATE_FORM_ID", () => {
    expect(VOUCHER_CREATE_FORM_ID).toBeDefined();
  });
});
