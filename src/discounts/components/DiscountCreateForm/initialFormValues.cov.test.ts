import { initialFormValues } from "./initialFormValues";

describe("discounts/components/DiscountCreateForm/initialFormValues.ts", () => {
  it("should export initialFormValues", () => {
    expect(initialFormValues).toBeDefined();
  });
});
