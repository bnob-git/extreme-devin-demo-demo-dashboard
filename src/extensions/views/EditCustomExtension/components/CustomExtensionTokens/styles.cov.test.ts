import { useStyles } from "./styles";

describe("extensions/views/EditCustomExtension/components/CustomExtensionTokens/styles.ts", () => {
  it("should export useStyles", () => {
    expect(useStyles).toBeDefined();
  });
});
