import { messages } from "./message";

describe("extensions/views/ViewManifestExtension/components/AppPage/message.ts", () => {
  it("should export messages", () => {
    expect(messages).toBeDefined();
  });
});
