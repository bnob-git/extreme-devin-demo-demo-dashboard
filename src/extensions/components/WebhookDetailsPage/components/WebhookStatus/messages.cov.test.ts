import { messages } from "./messages";

describe("extensions/components/WebhookDetailsPage/components/WebhookStatus/messages.ts", () => {
  it("should export messages", () => {
    expect(messages).toBeDefined();
  });
});
