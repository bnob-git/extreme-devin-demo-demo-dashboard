import { messages } from "./messages";

describe("extensions/components/WebhookDetailsPage/components/WebhookInfo/messages.ts", () => {
  it("should export messages", () => {
    expect(messages).toBeDefined();
  });
});
