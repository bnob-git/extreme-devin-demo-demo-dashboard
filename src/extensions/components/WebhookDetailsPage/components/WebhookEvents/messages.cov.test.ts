import { messages } from "./messages";

describe("extensions/components/WebhookDetailsPage/components/WebhookEvents/messages.ts", () => {
  it("should export messages", () => {
    expect(messages).toBeDefined();
  });
});
