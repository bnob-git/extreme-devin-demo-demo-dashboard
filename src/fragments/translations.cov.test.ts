import {
  attributeChoicesTranslationFragment,
  attributeTranslationDetailsFragment,
  attributeTranslationFragment,
  attributeValueTranslatableFragment,
  categoryTranslationFragment,
  collectionTranslationFragment,
  menuItemTranslationFragment,
  pageTranslatableFragment,
  pageTranslationFragment,
  productTranslationFragment,
  productVariantTranslationFragment,
  saleTranslationFragment,
  shippingMethodTranslationFragment,
  voucherTranslationFragment,
} from "./translations";

describe("fragments/translations.ts", () => {
  it("should export attributeChoicesTranslationFragment", () => {
    expect(attributeChoicesTranslationFragment).toBeDefined();
  });

  it("should export attributeTranslationDetailsFragment", () => {
    expect(attributeTranslationDetailsFragment).toBeDefined();
  });

  it("should export attributeTranslationFragment", () => {
    expect(attributeTranslationFragment).toBeDefined();
  });

  it("should export attributeValueTranslatableFragment", () => {
    expect(attributeValueTranslatableFragment).toBeDefined();
  });

  it("should export categoryTranslationFragment", () => {
    expect(categoryTranslationFragment).toBeDefined();
  });

  it("should export collectionTranslationFragment", () => {
    expect(collectionTranslationFragment).toBeDefined();
  });

  it("should export menuItemTranslationFragment", () => {
    expect(menuItemTranslationFragment).toBeDefined();
  });

  it("should export pageTranslatableFragment", () => {
    expect(pageTranslatableFragment).toBeDefined();
  });

  it("should export pageTranslationFragment", () => {
    expect(pageTranslationFragment).toBeDefined();
  });

  it("should export productTranslationFragment", () => {
    expect(productTranslationFragment).toBeDefined();
  });

  it("should export productVariantTranslationFragment", () => {
    expect(productVariantTranslationFragment).toBeDefined();
  });

  it("should export saleTranslationFragment", () => {
    expect(saleTranslationFragment).toBeDefined();
  });

  it("should export shippingMethodTranslationFragment", () => {
    expect(shippingMethodTranslationFragment).toBeDefined();
  });

  it("should export voucherTranslationFragment", () => {
    expect(voucherTranslationFragment).toBeDefined();
  });
});
