import { columnsMessages } from "./messages";

describe("categories/components/CategoryProductListDatagrid/messages.ts", () => {
  it("should export columnsMessages", () => {
    expect(columnsMessages).toBeDefined();
  });
});
