import { searchPermissionGroups } from "./usePermissionGroupSearch";

describe("searches/usePermissionGroupSearch.ts", () => {
  it("should export searchPermissionGroups", () => {
    expect(searchPermissionGroups).toBeDefined();
  });
});
