import { searchAttributes } from "./useAttributeSearch";

describe("searches/useAttributeSearch.ts", () => {
  it("should export searchAttributes", () => {
    expect(searchAttributes).toBeDefined();
  });
});
