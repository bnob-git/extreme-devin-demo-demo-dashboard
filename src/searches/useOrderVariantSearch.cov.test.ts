import { searchOrderVariant, useOrderVariantSearch } from "./useOrderVariantSearch";

describe("searches/useOrderVariantSearch.ts", () => {
  it("should export searchOrderVariant", () => {
    expect(searchOrderVariant).toBeDefined();
  });

  it("should export useOrderVariantSearch", () => {
    expect(useOrderVariantSearch).toBeDefined();
  });
});
