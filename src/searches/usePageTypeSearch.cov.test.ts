import { searchPageTypes } from "./usePageTypeSearch";

describe("searches/usePageTypeSearch.ts", () => {
  it("should export searchPageTypes", () => {
    expect(searchPageTypes).toBeDefined();
  });
});
