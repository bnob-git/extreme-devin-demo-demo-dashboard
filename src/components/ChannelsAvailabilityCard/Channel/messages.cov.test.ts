import { availabilityItemMessages } from "./messages";

describe("components/ChannelsAvailabilityCard/Channel/messages.ts", () => {
  it("should export availabilityItemMessages", () => {
    expect(availabilityItemMessages).toBeDefined();
  });
});
