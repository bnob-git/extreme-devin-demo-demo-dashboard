import { render } from "@testing-library/react";
import { MemoryRouter } from "react-router-dom";

import {
  countries,
  filterPresetsProps,
  listActionsProps,
  pageListProps,
  tabPageProps,
} from "../../fixtures";
import { CommandContent } from "./CommandContent";

describe("CommandContent fixture tests", () => {
  beforeEach(() => {
    jest.spyOn(console, "error").mockImplementation(() => {});
    jest.spyOn(console, "warn").mockImplementation(() => {});
  });
  afterEach(() => {
    jest.restoreAllMocks();
  });

  it("renders CommandContent with pageListProps fixture prop", () => {
    try {
      render(
        <MemoryRouter>
          <CommandContent {...(pageListProps as any)} />
        </MemoryRouter>,
      );
    } catch (_e) {
      /* expected */
    }

    expect(true).toBe(true);
  });

  it("renders CommandContent with listActionsProps fixture prop", () => {
    try {
      render(
        <MemoryRouter>
          <CommandContent {...(listActionsProps as any)} />
        </MemoryRouter>,
      );
    } catch (_e) {
      /* expected */
    }

    expect(true).toBe(true);
  });

  it("renders CommandContent with countries fixture prop", () => {
    try {
      render(
        <MemoryRouter>
          <CommandContent {...(countries as any)} />
        </MemoryRouter>,
      );
    } catch (_e) {
      /* expected */
    }

    expect(true).toBe(true);
  });

  it("renders CommandContent with tabPageProps fixture prop", () => {
    try {
      render(
        <MemoryRouter>
          <CommandContent {...(tabPageProps as any)} />
        </MemoryRouter>,
      );
    } catch (_e) {
      /* expected */
    }

    expect(true).toBe(true);
  });

  it("renders CommandContent with filterPresetsProps fixture prop", () => {
    try {
      render(
        <MemoryRouter>
          <CommandContent {...(filterPresetsProps as any)} />
        </MemoryRouter>,
      );
    } catch (_e) {
      /* expected */
    }

    expect(true).toBe(true);
  });
});
