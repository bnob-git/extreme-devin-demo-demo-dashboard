import { navigatorSearch } from "./queries";

describe("components/NavigatorSearch/queries.ts", () => {
  it("should export navigatorSearch", () => {
    expect(navigatorSearch).toBeDefined();
  });
});
