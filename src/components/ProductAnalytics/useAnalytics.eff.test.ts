// Auto-generated coverage test for useAnalytics.ts
jest.mock("posthog-js/react", () => {
  const proxy: any = new Proxy(function () {}, {
    get: (_: any, p: string) => {
      if (p === "__esModule") return true;

      if (p === "default") return proxy;

      if (p === "then") return undefined;

      return proxy;
    },
    apply: () => proxy,
    construct: () => proxy,
  });

  return proxy;
});

jest.mock("../Router/useRouteChange", () => {
  const proxy: any = new Proxy(function () {}, {
    get: (_: any, p: string) => {
      if (p === "__esModule") return true;

      if (p === "default") return proxy;

      if (p === "then") return undefined;

      return proxy;
    },
    apply: () => proxy,
    construct: () => proxy,
  });

  return proxy;
});

import { useAnalytics } from "./useAnalytics";

describe("useAnalytics", () => {
  it("exports useAnalytics", () => {
    expect(typeof useAnalytics).toBe("function");
  });
});
