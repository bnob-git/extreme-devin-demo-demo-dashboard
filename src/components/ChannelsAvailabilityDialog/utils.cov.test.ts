import { useChannelsSearch } from "./utils";

describe("components/ChannelsAvailabilityDialog/utils.ts", () => {
  it("should export useChannelsSearch", () => {
    expect(useChannelsSearch).toBeDefined();
  });
});
