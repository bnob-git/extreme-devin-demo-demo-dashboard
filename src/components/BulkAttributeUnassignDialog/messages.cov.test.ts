import defaultExport from "./messages";

describe("components/BulkAttributeUnassignDialog/messages.ts", () => {
  it("should have default export", () => {
    expect(defaultExport).toBeDefined();
  });
});
