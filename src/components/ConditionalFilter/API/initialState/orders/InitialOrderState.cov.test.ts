import { InitialOrderStateResponse } from "./InitialOrderState";

describe("InitialOrderState coverage", () => {
  it("accesses InitialOrderStateResponse", () => {
    expect(InitialOrderStateResponse).toBeDefined();
  });
});
