// Auto-generated coverage test for EventEmitter.ts
jest.mock("@saleor/macaw-ui-next", () => {
  const proxy: any = new Proxy(function () {}, {
    get: (_: any, p: string) => {
      if (p === "__esModule") return true;

      if (p === "default") return proxy;

      if (p === "then") return undefined;

      return proxy;
    },
    apply: () => proxy,
    construct: () => proxy,
  });

  return proxy;
});

jest.mock("./types", () => {
  const proxy: any = new Proxy(function () {}, {
    get: (_: any, p: string) => {
      if (p === "__esModule") return true;

      if (p === "default") return proxy;

      if (p === "then") return undefined;

      return proxy;
    },
    apply: () => proxy,
    construct: () => proxy,
  });

  return proxy;
});

import { FilterEventEmitter } from "./EventEmitter";

describe("EventEmitter", () => {
  it("exports FilterEventEmitter", () => {
    expect(FilterEventEmitter).toBeDefined();
  });
});
