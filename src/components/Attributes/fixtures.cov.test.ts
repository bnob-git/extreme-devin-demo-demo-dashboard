import { ATTRIBUTES, ATTRIBUTES_SELECTED } from "./fixtures";

describe("components/Attributes/fixtures.ts", () => {
  it("should export ATTRIBUTES", () => {
    expect(ATTRIBUTES).toBeDefined();
  });

  it("should export ATTRIBUTES_SELECTED", () => {
    expect(ATTRIBUTES_SELECTED).toBeDefined();
  });
});
