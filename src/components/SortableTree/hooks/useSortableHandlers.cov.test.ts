import { useSortableHandlers } from "./useSortableHandlers";

describe("components/SortableTree/hooks/useSortableHandlers.ts", () => {
  it("should export useSortableHandlers", () => {
    expect(useSortableHandlers).toBeDefined();
  });
});
