import { useAnnouncement } from "./useAnnouncement";

describe("components/SortableTree/hooks/useAnnouncement.ts", () => {
  it("should export useAnnouncement", () => {
    expect(useAnnouncement).toBeDefined();
  });
});
