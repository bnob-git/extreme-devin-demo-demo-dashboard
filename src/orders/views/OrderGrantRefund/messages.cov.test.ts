import { orderGrantRefundMessages } from "./messages";

describe("orders/views/OrderGrantRefund/messages.ts", () => {
  it("should export orderGrantRefundMessages", () => {
    expect(orderGrantRefundMessages).toBeDefined();
  });
});
