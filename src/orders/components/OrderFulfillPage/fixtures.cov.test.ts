import { orderToFulfill } from "./fixtures";

describe("orders/components/OrderFulfillPage/fixtures.ts", () => {
  it("should export orderToFulfill", () => {
    expect(orderToFulfill).toBeDefined();
  });
});
