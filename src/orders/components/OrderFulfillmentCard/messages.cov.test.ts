import { actionButtonsMessages } from "./messages";

describe("orders/components/OrderFulfillmentCard/messages.ts", () => {
  it("should export actionButtonsMessages", () => {
    expect(actionButtonsMessages).toBeDefined();
  });
});
