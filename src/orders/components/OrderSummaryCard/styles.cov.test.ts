import { useSummaryLineStyles } from "./styles";

describe("orders/components/OrderSummaryCard/styles.ts", () => {
  it("should export useSummaryLineStyles", () => {
    expect(useSummaryLineStyles).toBeDefined();
  });
});
