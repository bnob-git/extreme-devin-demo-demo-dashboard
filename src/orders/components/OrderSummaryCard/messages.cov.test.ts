import { orderSummaryMessages } from "./messages";

describe("orders/components/OrderSummaryCard/messages.ts", () => {
  it("should export orderSummaryMessages", () => {
    expect(orderSummaryMessages).toBeDefined();
  });
});
