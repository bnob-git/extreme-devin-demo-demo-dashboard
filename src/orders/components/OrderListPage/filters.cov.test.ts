import { OrderFilterKeys } from "./filters";

describe("orders/components/OrderListPage/filters.ts", () => {
  it("should export OrderFilterKeys", () => {
    expect(OrderFilterKeys).toBeDefined();
  });
});
