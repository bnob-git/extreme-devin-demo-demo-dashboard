import { orderReturnMessages } from "./messages";

describe("orders/components/OrderReturnPage/messages.ts", () => {
  it("should export orderReturnMessages", () => {
    expect(orderReturnMessages).toBeDefined();
  });
});
