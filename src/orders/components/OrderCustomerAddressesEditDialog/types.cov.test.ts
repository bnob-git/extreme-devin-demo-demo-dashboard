import { AddressEditDialogVariant } from "./types";

describe("orders/components/OrderCustomerAddressesEditDialog/types.ts", () => {
  it("should export AddressEditDialogVariant", () => {
    expect(AddressEditDialogVariant).toBeDefined();
  });
});
